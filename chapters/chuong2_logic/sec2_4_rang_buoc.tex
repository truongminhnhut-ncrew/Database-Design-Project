% !TEX root = ../../main.tex
\section{Thiết kế ràng buộc toàn vẹn mức logic}
\label{sec:2_4_rang_buoc_logic}

\subsection{Ràng buộc thực thể (Entity Integrity)}
% Nội dung ràng buộc thực thể (PK NOT NULL, UNIQUE)

\subsection{Ràng buộc tham chiếu (Referential Integrity)}
% Nội dung ràng buộc tham chiếu (FK, ON DELETE CASCADE, SET NULL)

\subsection{Ràng buộc miền giá trị (Domain Integrity)}
% Nội dung ràng buộc miền giá trị (CHECK constraints, ENUM)
